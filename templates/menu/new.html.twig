{% extends 'base.html.twig' %}

{% block title %}Nouveau Menu{% endblock %}

{% block body %}
<div class="container">
    <h1>Nouveau Menu</h1>
    {{ include('menu/_form.html.twig') }}
    <a class="btn btn-primary"href="{{ path('app_menu_index') }}">Retour</a>
</div>
{% endblock %}
