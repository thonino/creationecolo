{% extends 'base.html.twig' %}

{% block title %}Menu{% endblock %}

{% block body %}
<div class="container">
    <h1>Menu</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ menu.name }}</td>
            </tr>
        </tbody>
    </table>
    <a class="btn btn-primary mx-1" href="{{ path('app_menu_index') }}">back to list</a>
</div>
{% endblock %}
